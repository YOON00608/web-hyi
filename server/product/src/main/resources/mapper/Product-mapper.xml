<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.korea.product.dao.ProductDAO">

    <!-- 전체 상품 조회 -->
    <select id="selectAllProducts" resultType="ProductVO">
        SELECT product_id, product_name, product_stock, product_price, register_date, update_date
        FROM product
        ORDER BY product_id DESC
    </select>
    
    <!-- 상품 등록 -->
    <insert id="insertProduct" parameterType="ProductVO" keyProperty="productId">
        INSERT INTO product (product_name, product_stock, product_price)
        VALUES (#{productName}, #{productStock}, #{productPrice})
    </insert>

    <!-- 상품 상세 조회 -->
    <select id="selectProductById" parameterType="int" resultType="ProductVO">
        SELECT product_id, product_name, product_stock, product_price, register_date, update_date
        FROM product
        WHERE product_id = #{productId}
    </select>

</mapper>